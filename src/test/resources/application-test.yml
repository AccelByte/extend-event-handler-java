app:
  config:
    namespace: ${NAMESPACE:accelbyte}
    resource_name: 'MMV2GRPCSERVICE'
    item_id_to_grant: ${ITEM_ID_TO_GRANT:""}
  security:
    client_id: '123'
    client_secret: 'abc'

justice:
  grpc:
    interceptor:
      auth:
        enabled: true
      debug-logger:
        enabled: false